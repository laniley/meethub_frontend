<div class="main-area">
  <div class="container-fluid bugreport-container">
    <h1>Report a bug</h1>
    <div class="row">
      <div class="col-xs-10 col-xs-offset-1 col-md-6 col-md-offset-3 bugreport-text">
        {{#if isGerman}}
          {{textarea id="bugreport-text" autofocus="autofocus" placeholder="Bitte beschreibe den Fehler so genau wie möglich..." rows="5" maxlength="2500" value=description}}
        {{else}}
          {{textarea id="bugreport-text" autofocus="autofocus" placeholder="Please describe the bug..." rows="5" maxlength="2500" value=description}}
        {{/if}}
      </div>
    </div>
    <div class="row button-row"><button {{action "sendMail"}}>{{t "send"}}</button></div>
  </div>
</div>