<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      {{#link-to 'index'}}
        <div class="navbar-brand">
          <img alt="Brand" src="assets/images/logo_square_120x120.png">
          <div class="brand-name">MEETHUB <span class="beta-label">BETA</span></div>
        </div>
      {{/link-to}}
    </div>
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <!-- Collect the nav links, forms, and other content for toggling -->
      {{!-- CENTER AREA / TOP-NAV-BAR --}}
      <ul class="nav navbar-nav center-links">
        <li>
          {{#link-to 'index.events'}}
            <span>{{fa-icon "calendar" fixedWith=true}}</span>
            <span class="link-label hidden-sm">{{t 'calendar'}}</span>
            {{#if hasUnreadEventInvitations}}
              <span class="info-counter">{{number_of_new_event_invitations}}</span>
            {{/if}}
          {{/link-to}}
        </li>
        {{!--
          <li>
            {{#link-to 'members-area.meethubs'}}
              <span>{{unbound fa-icon "fa-fixed-with  fa-share-alt"}}</span>
              <span class="link-label hidden-sm">Meethubs</span>
              {{#if hasNewMeethubInfos}}
                <span class="info-counter">{{newMeethubInfosCount}}</span>
              {{/if}}
            {{/link-to}}
          </li>
        --}}
        <li>
          {{#link-to 'index.friendships'}}
            <span>{{fa-icon "group" fixedWith=true}}</span>
            <span class="link-label hidden-sm">{{t 'friend.title'}}</span>
            {{#if hasUnseenNewFriendships}}
              <span class="info-counter">{{number_of_unseen_new_friendships}}</span>
            {{/if}}
          {{/link-to}}
        </li>
        {{!-- <li>
          {{#link-to 'members-area.locations'}}
            <span>{{unbound fa-icon "fa-fixed-with fa-compass"}}</span>
            <span class="link-label hidden-sm">Locations</span>
          {{/link-to}}
        </li> --}}
      </ul>
      {{!-- RIGHT AREA --}}
      <ul class="nav navbar-nav navbar-right">
        {{!-- <li class="custom-link">
          {{#link-to 'members-area.messages'}}
            <span {{bind-attr class=":link-icon"}}>{{unbound fa-icon "fa-fixed-with fa-envelope"}}</span>
            <span class="link-label visible-xs">{{t 'messages.title'}}</span>
            {{#if hasUnreadMessages}}
              <span class="info-counter">{{unreadMessages.length}}</span>
            {{/if}}
          {{/link-to}}
        </li> --}}
        <li class="custom-link fb-sync-icon {{isSyncing}}" {{action "resyncWithFB"}}>
          <span class="fa-stack">
            {{#if isSyncing}}
              {{fa-icon "refresh" spin=true stack=2 size=3}}
            {{else}}
              {{fa-icon "refresh" stack=2 size=3}}
            {{/if}}
            {{fa-icon "facebook-square" stack=1}}
          </span>
          <span class="link-label visible-xs">{{t "resync-with-fb"}}</span>
        </li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">
            <div class="account_img_container">
              <img src={{model.picture}} class="account_img" />
            </div>
            <div class="account_name">
              {{model.name}}
            </div>
          </a>
          <ul class="dropdown-menu" role="menu">
            <li {{action "logout"}}><a href="#">{{fa-icon "sign-out"}} Log Out</a></li>
          </ul>
        </li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
