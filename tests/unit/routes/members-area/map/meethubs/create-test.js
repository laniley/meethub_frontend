import {
  moduleFor,
  test
} from 'ember-qunit';

moduleFor('route:members-area/map/meethubs/create', 'MembersAreaMapMeethubsCreateRoute', {
  // Specify the other units that are required for this test.
  // needs: ['controller:foo']
});

test('it exists', function() {
  var route = this.subject();
  ok(route);
});
